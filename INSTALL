SURVEYS MANAGER INSTALLATION
============================

# Dependencies

 This software needs following libraries to run:
   * libsqlite3: in debian distribution, install the libsqlite3-dev
                 package through aptitude.
   * libopenair: see relative project repository for the installation
                 instructions.

 To build the project you need: autotools and c++14.

# Build and install

 Run following commands:
   > ./configure
   > make
   > sudo make install
